{
  "_schema_version": "1.0",
  "_description": "Schema de secrets requeridos por el sistema ARGOS. Ambos servicios (data-engine TS, analytics-engine PY) leen este mismo archivo para preflight check.",
  "production": [
    "EXCHANGE_API_KEY",
    "EXCHANGE_API_SECRET",
    "ARGOS_BROKER_URL"
  ],
  "testnet": [
    "BINANCE_TESTNET_API_KEY",
    "BINANCE_TESTNET_SECRET"
  ],
  "_testnet_note": "These are Binance Futures DEMO keys (demo-fapi.binance.com), not Spot testnet",
  "notifications": [
    "TELEGRAM_BOT_TOKEN",
    "TELEGRAM_CHAT_ID"
  ],
  "optional": [
    "EXCHANGE_PASSPHRASE",
    "EXCHANGE_ID",
    "EXCHANGE_WS_URL",
    "DISCORD_WEBHOOK_URL",
    "USE_PYTORCH",
    "RISK_PCT",
    "DRAWDOWN_PCT",
    "ARGOS_ENV_MODE_FILE",
    "ARGOS_HISTORICAL_DIR"
  ],
  "mode_requirements": {
    "BACKTESTING": {
      "required_groups": [],
      "description": "No se requieren secrets. Todo es mockeado."
    },
    "PAPER_TRADING": {
      "required_groups": ["testnet"],
      "description": "Requiere testnet. Notificaciones opcionales."
    },
    "LIVE": {
      "required_groups": ["production", "notifications"],
      "description": "Requiere todo. Sin fallback ni defaults."
    }
  }
}
