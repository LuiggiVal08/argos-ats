# Analytics Engine (FastAPI) - copy to .env, do not commit
# Broker is any RESP-compatible implementation:
#   Redis 7+ / Memurai (Windows) / Dragonfly / Valkey / KeyDB / Garnet / Redict

# --- Required (all modes) ---
ARGOS_BROKER_URL=redis://localhost:6379
ENVIRONMENT_MODE=PAPER_TRADING

# --- Required for LIVE mode ---
# If ENVIRONMENT_MODE=LIVE and these are missing/empty, init aborts.
EXCHANGE_API_KEY=
EXCHANGE_API_SECRET=

# --- Binance Futures Demo (PAPER_TRADING / LIVE) ---
# When BINANCE_TESTNET=true, the engine uses Binance USDⓈ-M Futures
# Demo (demo-fapi.binance.com) with EXCHANGE_ID as exchange type.
# Requires these vars (non-empty):
BINANCE_TESTNET_API_KEY=
BINANCE_TESTNET_SECRET=
BINANCE_TESTNET=true

# --- Optional ---
EXCHANGE_PASSPHRASE=          # Required by some exchanges (e.g. OKX)
EXCHANGE_ID=binanceusdm       # Default: binanceusdm (Binance USDⓈ-M futures)
USE_PYTORCH=true              # Set to 'true' to use PyTorch adapter instead of Keras

# --- Notifications (webhooks) ---
# These are consumed by the data-engine (Node/NestJS) which dispatches
# to Telegram/Discord. If none are set, notifications are no-op.
TELEGRAM_BOT_TOKEN=           # Bot token from @BotFather
TELEGRAM_CHAT_ID=             # Chat/channel ID to receive alerts
DISCORD_WEBHOOK_URL=          # Discord webhook URL

# --- Execution thresholds (regime-aware Policy D) ---
# These control the minimum confidence required to execute a trade.
# The regime (TRENDING / RANGING) is detected by the inference pipeline (ADX ≥25).
# If a variable is unset or empty, the code fallback is used:
#   TRENDING=0.55, RANGING=0.62
EXECUTION_THRESHOLD_TRENDING=0.55  # Confidence floor in trending markets
EXECUTION_THRESHOLD_RANGING=0.62   # Confidence floor in ranging markets

# --- Risk defaults ---
RISK_PCT=0.01                 # 1% max risk per trade
DRAWDOWN_PCT=0.05             # 5% max daily drawdown before circuit breaker
